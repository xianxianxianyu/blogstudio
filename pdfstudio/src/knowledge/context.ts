/**
 * `Context` 的规范定义住在知识库那边，这里只转出去。
 *
 * 此前这里是一份**手抄的副本**，而且已经抄歪了：`source` 是字符串 `"page 3"`，
 * 而正式定义早就把它改成了结构化的 `Source`（docId + 书名 + 页码）——理由写在那边：
 * 「在跨 PDF 的知识库里，不带文档名的页码等于没有出处，而『可回跳』是 context 的
 * 硬标准之一」。
 *
 * 两份定义各自漂移正是 ADR-0002 拒绝「传输格式」的理由：**类型变了两边一起改**，
 * 因为两个产品住在同一个仓库里。所以这里不再有第二份，只有一个 re-export。
 */
export type { Context, Source } from "../../../contextstudio/src/context";
