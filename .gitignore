# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# typescript
*.tsbuildinfo

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

/dist/
/outputs/
/work/

# local issue tracker (see docs/agents/issue-tracker.md)
.scratch/

# real model credentials (ADR-0005: key never enters source or build output)
/pdfstudio/config.json

# dev 形态的数据根就是 pdfstudio/（`vite.app.config.ts` 的 paths）。去处与同步账本
# 是这台机器的东西：仓库路径、ssh 别名、往哪台服务器同步——不进仓库。
/pdfstudio/destinations.json
/pdfstudio/published.json

# PDF Studio 的书架：是你的书和阅读记录，不是仓库内容
pdfstudio/.library/

# 本地模型权重：几百 MB，不进仓库
pdfstudio/.models/

# Electron 构建产物
pdfstudio/dist/
pdfstudio/release/

# 两份可重建的索引（ADR-0005 决策 6：删了能从文件重扫出来）。dev 的数据根就是
# pdfstudio/，所以它们落在这儿；此前 blog-index.db 被跟踪了，每列一次文章仓库就脏。
pdfstudio/blog-index.db*
pdfstudio/projects-index.db*
