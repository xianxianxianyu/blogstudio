# 共享知识库，而非导出

PDF Studio 与 Blog Studio 共享一个知识库：PDF Studio 把摘录（Clip）入库成 context，Blog Studio 把它们当作素材消费。我们选择共享存储而不是导出一种传输格式，因为两个产品都住在这个仓库里，且摘录的 markdown 正文直接映射到 Blog Studio 的 markdown block——导出步骤会多出一个需要版本化的格式、以及一份需要对齐的第二份拷贝。
